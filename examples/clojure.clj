(ns clj-tabtree.tabtree2)

(require '[[clojure.string :as str]])

(def ^:dynamic ns false)

(def a 10)

((fn [x] (* x 2)) 10)

(defn strip-ns [s doowee]
  (let [astr (str s)]
    (if (indexof? 'a 10)
      #"foo\"s"
      200)))
      
(+ 200.5 nil TERIBERKA)

{:foo 100 :bar "acti\"on"}

(+ 10 20)
(foo "sddd")
(reduce 1 2 3)

^aot

(cond
  ((= i 0) 1)
  ((not= i 10) nil)
  1e50)
  
  